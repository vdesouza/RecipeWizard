<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:appNs="http://schemas.android.com/apk/res-auto">

    <!-- Favorites action -->
    <item android:id="@+id/action_favorites"
          android:icon="@drawable/ic_star"
          android:title="@string/favorites_icon_name"
          appNs:showAsAction="always|withText"/>


    <!-- Favorites action -->
    <item android:id="@+id/action_filters"
          android:icon="@drawable/ic_filter"
          android:title="@string/filters_icon_name"
          appNs:showAsAction="always|withText"/>



</menu>